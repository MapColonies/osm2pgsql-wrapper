export const command = 'append';

export const describe = 'update existing database from an osm change file';

export const APPEND_COMMAND_FACTORY = Symbol('AppendCommandFactory');

export const APPEND_MANAGER_FACTORY = Symbol('AppendManagerFactory');
